<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>co.nz.tmsandbox</groupId>
  <artifactId>tmsandbox-perf</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>TM Sandbox Categories API - Gatling Performance Tests</name>
  <description>
    Gatling (Java) performance test suite for the TM Sandbox Categories API.
    Covers NFR-01 through NFR-04 with configurable ramp-up, steady state,
    throughput, and VUser count.
  </description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <gatling.version>3.11.5</gatling.version>
    <gatling-maven-plugin.version>4.9.6</gatling-maven-plugin.version>

    <perf.vUsers>5</perf.vUsers>
    <perf.rampUpSeconds>5</perf.rampUpSeconds>
    <perf.steadyStateSeconds>60</perf.steadyStateSeconds>
    <perf.totalRequests>10</perf.totalRequests>
    <perf.p90ThresholdMs>500</perf.p90ThresholdMs>
    <perf.categoryIds>6327,6328,6329,6330,6331,6332,6333,6334,6335,6336</perf.categoryIds>
    <perf.csvOutput>results/category_results.csv</perf.csvOutput>
  </properties>

  <dependencies>
    <dependency>
      <groupId>io.gatling.highcharts</groupId>
      <artifactId>gatling-charts-highcharts</artifactId>
      <version>${gatling.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>2.17.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <testSourceDirectory>src/test/java</testSourceDirectory>
    <plugins>
      <plugin>
        <groupId>io.gatling</groupId>
        <artifactId>gatling-maven-plugin</artifactId>
        <version>${gatling-maven-plugin.version}</version>
        <configuration>
          <!-- Run headless (no interactive menu) -->
          <runMultipleSimulations>false</runMultipleSimulations>
          <simulationClass>simulations.CategoriesApiSimulation</simulationClass>
          <jvmArgs>
            <jvmArg>-Dperf.vUsers=${perf.vUsers}</jvmArg>
            <jvmArg>-Dperf.rampUpSeconds=${perf.rampUpSeconds}</jvmArg>
            <jvmArg>-Dperf.steadyStateSeconds=${perf.steadyStateSeconds}</jvmArg>
            <jvmArg>-Dperf.totalRequests=${perf.totalRequests}</jvmArg>
            <jvmArg>-Dperf.p90ThresholdMs=${perf.p90ThresholdMs}</jvmArg>
            <jvmArg>-Dperf.categoryIds=${perf.categoryIds}</jvmArg>
            <jvmArg>-Dperf.csvOutput=${perf.csvOutput}</jvmArg>
          </jvmArgs>
          <resultsFolder>${project.basedir}/reports</resultsFolder>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>test</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>17</release>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
